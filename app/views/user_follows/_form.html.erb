<%= form_for(@user_follow) do |f| %>
  <% if @user_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_follow.errors.count, "error") %> prohibited this user_follow from being saved:</h2>

      <ul>
      <% @user_follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :follower_id %><br>
    <%= f.number_field :follower_id %>
  </div>
  <div class="field">
    <%= f.label :followee_id %><br>
    <%= f.number_field :followee_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
