<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no" />
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= csrf_meta_tags %>
</head>

<body>
<%= subscribe_to "/inbox/#{current_user_id}" %>

<!--Wraps everything but the footer. This makes the footer sticky-->
<div id="content-wrap">

  <!--Navigation bar-->
  <%= render 'layouts/navigation_bar' %>

  <!--Log In-->
  <%= render 'layouts/log_in_portal' %> 

  <!--Template-->
  <div class="template-wrap <%= controller.controller_name %>">
    <%= yield %>
  </div>

</div>

<!--Bottom footer-->
<%= render 'layouts/footer' %>

</body>
</html>
