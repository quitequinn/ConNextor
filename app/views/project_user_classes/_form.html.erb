<%= form_for(@project_user_class) do |f| %>
  <% if @project_user_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_user_class.errors.count, "error") %> prohibited this project_user_class from being saved:</h2>

      <ul>
      <% @project_user_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
