<div class="element-contact">
  <%= form_for profile_contact, remote: true do |f| %>

      <%= render 'form_fields', f: f %>
      <%= f.submit class: 'btn-inverse btn-element-margin-match' %>
  <% end %>
</div>
