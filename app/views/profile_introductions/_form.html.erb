<%= form_for(@profile_introduction) do |f| %>
  <% if @profile_introduction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_introduction.errors.count, "error") %> prohibited this profile_introduction from being saved:</h2>

      <ul>
      <% @profile_introduction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :profile_id %><br>
    <%= f.number_field :profile_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
